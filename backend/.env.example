# Copy this file to .env and fill in your local values.
# Docker Compose overrides DATABASE_URL automatically — no .env edit needed for Docker.

DATABASE_URL=postgresql+psycopg://<user>:<password>@<host>:5432/ai-assistant-db
